{% import "_includes/forms" as forms %}

{{ forms.textareaField({
    label: "Value"|t,
    instructions: "The value of the hidden field. Twig is allowed."|t,
    id: 'value',
    name: 'value',
    value: settings.value ?? null,
    translatable: true,
    errors: settings.getErrors('value')
}) }}
