{% import '_includes/forms' as forms %}

{{ forms.selectField({
    label: 'Form'|t,
    instructions: 'Which form do you want to see submissions from?'|t,
    id: 'form',
    name: 'form',
    options: availableForms,
    value: settings.form,
    errors: settings.getErrors('form')
}) }}

{{ forms.textField({
    label: 'Limit'|t,
    id: 'limit',
    name: 'limit',
    value: settings.limit,
    size: 2,
    errors: settings.getErrors('limit')
}) }}

{{ forms.selectField({
    label: 'Date'|t,
    id: 'showDate',
    name: 'showDate',
    options: { '': 'Don’t show'|t, 'date': 'Show date'|t, 'dateTime': 'Show date and time'|t },
    value: settings.showDate,
    errors: settings.getErrors('showDate')
}) }}
